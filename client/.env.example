# Frontend Environment Variables (Vite)
# Note: Variables must start with VITE_ to be accessible in client-side code

# Backend API URL (for local dev, leave empty to use Vite proxy or set to http://localhost:5000)
VITE_API_URL=http://localhost:5000
